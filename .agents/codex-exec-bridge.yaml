name: codex-exec-bridge
description: Codex CLI / Codex Exec bridge — invokes OpenAI Codex with shared runtime, captures task output to .claude-bridge/, returns to caller.
version: 1.0.0
license: MIT
homepage: https://github.com/treylom/ThisCode
provides_tools:
  - claude_discode_codex_invoke
tier: experimental
model: opus[1m]
allowedTools:
  - Bash
  - Read
  - Write
