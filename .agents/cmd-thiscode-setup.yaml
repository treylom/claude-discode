name: cmd-thiscode-setup
description: Slash command /thiscode:setup — interactive bootstrap wizard (Tier 1-4 install choices), invokes thiscode-bootstrap skill.
version: 1.0.0
provides_commands:
  - /thiscode:setup
tier: external
allowedTools:
  - Bash
  - AskUserQuestion
  - Write
