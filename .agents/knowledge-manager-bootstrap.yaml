name: knowledge-manager-bootstrap
description: One-shot environment installer for the 4-Tier search stack — installs Obsidian CLI (optional), vault-search MCP, GraphRAG server (optional), records vault_root.
version: 1.0.0
license: MIT
homepage: https://github.com/treylom/claude-discode
provides_tools:
  - knowledge_manager_bootstrap
provides_commands:
  - /claude-discode:km-bootstrap
tier: external
model: opus[1m]
allowedTools:
  - Bash
  - AskUserQuestion
  - Write
  - Read
